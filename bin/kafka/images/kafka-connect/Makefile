DOCKER_USERNAME ?= sebge2
APPLICATION_NAME ?= kafka-connect
APPLICATION_VERSION ?= 1.1.0


build:
	docker build --tag ${DOCKER_USERNAME}/${APPLICATION_NAME}:${APPLICATION_VERSION} .

push:
	docker push ${DOCKER_USERNAME}/${APPLICATION_NAME}:${APPLICATION_VERSION}